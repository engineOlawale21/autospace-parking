# Legacy Prisma model notes

This directory belongs to the prototype persistence layer and is being retired.
Do not generate new Prisma modules, models, DTOs, or migrations.

The existing schema and migrations remain temporarily so modules can be
migrated to TypeORM with data and API parity. Follow the migration process in
[`docs/ARCHITECTURE.md`](../../../docs/ARCHITECTURE.md#persistence-migration).
